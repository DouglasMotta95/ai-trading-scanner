import test from'node:test';import assert from'node:assert/strict';import{ema,rsi,macd}from'../src/core/indicators.js';import{confluence}from'../src/core/confluence.js';import{nextSignalState}from'../src/core/signal-machine.js';import{CandleBuilder}from'../src/core/candles.js';test('ema returns finite value',()=>assert.ok(Number.isFinite(ema([1,2,3,4,5],3))));test('rsi rising series is 100',()=>assert.equal(rsi(Array.from({length:16},(_,i)=>i+1),14),100));test('macd returns line signal histogram',()=>{const m=macd(Array.from({length:60},(_,i)=>i+1));assert.ok(Number.isFinite(m.macd));assert.ok(Number.isFinite(m.signal));assert.ok(Number.isFinite(m.histogram))});test('confluence grades weighted checks',()=>{const c=confluence([{label:'a',weight:50,passed:true},{label:'b',weight:50,passed:false}]);assert.equal(c.score,50);assert.equal(c.confirmations,1)});test('signal machine confirms strong directional setup',()=>assert.equal(nextSignalState(null,{state:'WATCH',score:90,direction:'BUY'},{connected:true,scanning:true,stale:false}).state,'CONFIRM'));test('candle builder closes previous bucket',()=>{const b=new CandleBuilder(60000);assert.equal(b.push(10,0),null);b.push(12,1000);const c=b.push(11,60000);assert.equal(c.open,10);assert.equal(c.high,12);assert.equal(c.close,12);assert.equal(b.snapshot().closed.length,1)});