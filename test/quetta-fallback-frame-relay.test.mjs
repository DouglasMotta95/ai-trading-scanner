import test from 'node:test';
import assert from 'node:assert/strict';
import fs from 'node:fs';
const read = path => fs.readFileSync(new URL(`../${path}`, import.meta.url), 'utf8');
test('reconnect injects observable Quetta recovery before ordinary live readers', () => { const injector=read('src/background-modern-injector.js'); const runtimeIndex=injector.indexOf("'src/content/runtime-message-compat.js'"); const bootIndex=injector.indexOf("'src/content/runtime-boot-probe.js'"); const bootstrapIndex=injector.indexOf("'src/content/page-world-bootstrap.js'"); const topIndex=injector.indexOf("'src/content/opaque-frame-top-bridge.js'"); const recoveryIndex=injector.indexOf("'src/content/opaque-frame-recovery.js'"); const focusIndex=injector.indexOf("'src/content/focused-asset-v2.js'"); assert.ok(runtimeIndex>=0); assert.ok(bootIndex>runtimeIndex); assert.ok(bootstrapIndex>bootIndex); assert.ok(topIndex>bootstrapIndex); assert.ok(recoveryIndex>topIndex); assert.ok(focusIndex>recoveryIndex); assert.match(injector,/executeScriptCompat/); assert.match(injector,/chrome\.scripting\.executeScript\(details, callback\)/); assert.match(injector,/returned && typeof returned\.then === 'function'/); assert.match(injector,/default-world-fallback/); assert.match(injector,/runtimeInjection/); });
test('opaque recovery accepts Quetta sender quirks without opening the proxy to normal pages', () => { const recovery=read('src/content/opaque-frame-recovery.js'); const proxy=read('src/background-opaque-frame-proxy.js'); const top=read('src/content/opaque-frame-top-bridge.js'); assert.match(recovery,/\['blob:', 'about:', 'data:'\]/); assert.match(recovery,/ATS_OPAQUE_FRAME_PROXY/); assert.match(recovery,/ATS_VISUAL_FOCUS_V2/); assert.match(recovery,/ATS_EMBEDDED_FEED/); assert.match(recovery,/ATS_CHART_FRAME_MARKET/); assert.match(recovery,/ATS_MARKET_CLOCK_V2/); assert.match(recovery,/ATS_DATA_INSPECTOR/); assert.match(recovery,/ATS_ACCOUNT_METRICS/); assert.match(proxy,/sender\.tab\?\.url/); assert.match(proxy,/!senderUrl/); assert.match(proxy,/sender\.origin \|\| ''\) === 'null'/); assert.match(proxy,/!casaHost\(topHost\)/); assert.match(proxy,/!senderOpaque/); assert.doesNotMatch(proxy,/frameId === 0/); assert.match(top,/window !== window\.top/); assert.match(top,/ATS_OPAQUE_FRAME_FORWARD/); });
test('opaque recovery recognizes standalone instruments without inventing a new signal engine', () => { const recovery=read('src/content/opaque-frame-recovery.js'); const background=read('src/background-entry.js'); assert.match(recovery,/\['TRON','TRX\/USD'\]/); assert.match(recovery,/\['EURO','EUR\/USD'\]/); assert.match(recovery,/opaque-frame-recovery/); assert.match(recovery,/network-server-cycle/); assert.match(background,/background-runtime-telemetry\.js/); assert.match(background,/background-opaque-frame-proxy\.js/); assert.match(background,/background-market-session\.js/); assert.doesNotMatch(recovery,/processSnapshot|possibleScore|confirmScore|candleStrength|rejectionStrength/); });
test('page-world fallback is packaged for browsers that reject explicit MAIN world injection', () => { const manifest=JSON.parse(read('manifest.json')); const bootstrap=read('src/content/page-world-bootstrap.js'); const sentinel=read('src/content/page-world-sentinel.js'); const resources=manifest.web_accessible_resources?.flatMap(row=>row.resources||[])||[]; assert.equal(manifest.version,'0.11.11'); assert.ok(resources.includes('src/content/page-world-sentinel.js')); assert.ok(resources.includes('src/content/canvas-countdown-probe.js')); assert.ok(resources.includes('src/content/network-probe.js')); assert.ok(resources.includes('src/content/worker-probe.js')); assert.match(bootstrap,/runtime\.getURL\(file\)/); assert.match(bootstrap,/page-world-confirmed/); assert.match(bootstrap,/ATS_RUNTIME_BOOT/); assert.match(sentinel,/ATS_PAGE_WORLD_SENTINEL/); });
