export class CandleBuilder{constructor(timeframeMs=60000){this.timeframeMs=timeframeMs;this.current=null;this.closed=[]}push(price,timestamp=Date.now()){price=Number(price);if(!Number.isFinite(price))return null;const bucket=Math.floor(timestamp/this.timeframeMs)*this.timeframeMs;if(!this.current||this.current.time!==bucket){const closed=this.current;this.current={time:bucket,open:price,high:price,low:price,close:price,ticks:1};if(closed){this.closed.push(Object.freeze({...closed}));if(this.closed.length>1000)this.closed.shift()}return closed||null}this.current.high=Math.max(this.current.high,price);this.current.low=Math.min(this.current.low,price);this.current.close=price;this.current.ticks++;return null}seed(candles=[]){if(!Array.isArray(candles)||!candles.length)return 0;const byTime=new Map(this.closed.map(c=>[Number(c.time),c]));for(const raw of candles){let t=Number(raw?.time??raw?.timestamp);if(Number.isFinite(t)&&t>0&&t<1e12)t*=1000;if(!Number.isFinite(t)||t<=0)continue;const time=Math.floor(t/this.timeframeMs)*this.timeframeMs;const open=Number(raw?.open),high=Number(raw?.high),low=Number(raw?.low),close=Number(raw?.close);if(![open,high,low,close].every(Number.isFinite))continue;if(this.current&&time>=this.current.time)continue;byTime.set(time,Object.freeze({time,open,high,low,close,ticks:Number(raw?.ticks)||1}))}this.closed=[...byTime.values()].sort((a,b)=>a.time-b.time).slice(-1000);return this.closed.length}snapshot(){return{current:this.current?{...this.current}:null,closed:[...this.closed]}}}
export const TIMEFRAMES={S5:5000,S15:15000,S30:30000,M1:60000,M2:120000,M5:300000,M15:900000,M30:1800000,H1:3600000,H4:14400000};