export function confluence(checks=[]){const valid=checks.filter(c=>c&&Number.isFinite(c.weight));const max=valid.reduce((a,c)=>a+Math.max(0,c.weight),0);const earned=valid.reduce((a,c)=>a+(c.passed?Math.max(0,c.weight):0),0);const score=max?Math.round(earned/max*100):0;return{score,grade:score>=85?'A+':score>=75?'A':score>=65?'B':'C',confirmations:valid.filter(c=>c.passed).length,total:valid.length,reasons:valid.filter(c=>c.passed).map(c=>c.label),missing:valid.filter(c=>!c.passed).map(c=>c.label)}}
