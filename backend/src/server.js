import http from'node:http';import crypto from'node:crypto';import fs from'node:fs';import path from'node:path';import{fileURLToPath}from'node:url';const __dirname=path.dirname(fileURLToPath(import.meta.url)),PORT=Number(process.env.PORT||8787),API_KEY=process.env.ATS_API_KEY||'',ADMIN_KEY=process.env.ATS_ADMIN_KEY||API_KEY,ALLOWED_ORIGINS=(process.env.CORS_ORIGINS||'').split(',').map(x=>x.trim()).filter(Boolean),DATA_DIR=path.resolve(__dirname,'../data'),LICENSE_FILE=path.join(DATA_DIR,'licenses.json'),ADMIN_DIR=path.resolve(__dirname,'../../apps/admin-dashboard'),sessions=new Map(),events=[],licenses=new Map(),PLANS={trial:{id:'trial',label:'Trial',dailySignals:Number(process.env.PLAN_TRIAL_SIGNALS||3),deviceLimit:1},starter:{id:'starter',label:'Starter',dailySignals:Number(process.env.PLAN_STARTER_SIGNALS||6),deviceLimit:1},pro:{id:'pro',label:'Pro',dailySignals:Number(process.env.PLAN_PRO_SIGNALS||20),deviceLimit:Number(process.env.PLAN_PRO_DEVICES||2)},unlimited:{id:'unlimited',label:'Unlimited',dailySignals:null,deviceLimit:Number(process.env.PLAN_UNLIMITED_DEVICES||5)}};const isObj=v=>v&&typeof v==='object'&&!Array.isArray(v),str=(v,n=128)=>typeof v==='string'&&v.length>0&&v.length<=n,origin=req=>{const o=req.headers.origin;if(!o)return'';return ALLOWED_ORIGINS.includes(o)?o:null},headers=req=>{const o=origin(req),h={'content-type':'application/json','access-control-allow-headers':'content-type,x-api-key,x-admin-key','access-control-allow-methods':'GET,POST,OPTIONS','vary':'Origin'};if(o)h['access-control-allow-origin']=o;return h},json=(req,res,status,data)=>{res.writeHead(status,headers(req));res.end(status===204?'':JSON.stringify(data))},body=req=>new Promise((resolve,reject)=>{let d='';req.on('data',c=>{d+=c;if(d.length>65536){reject(new Error('too_large'));req.destroy()}});req.on('end',()=>{try{resolve(d?JSON.parse(d):{})}catch{reject(new Error('invalid_json'))}})}),safeEq=(raw,key)=>{if(!key||typeof raw!=='string')return false;const a=Buffer.from(raw),b=Buffer.from(key);return a.length===b.length&&crypto.timingSafeEqual(a,b)},auth=req=>safeEq(req.headers['x-api-key'],API_KEY),adminAuth=req=>safeEq(req.headers['x-admin-key'],ADMIN_KEY),only=(o,keys)=>Object.keys(o).every(k=>keys.includes(k)),today=()=>new Date().toISOString().slice(0,10),now=()=>Date.now(),publicLicense=l=>{const plan=PLANS[l.plan]||PLANS.starter,used=Number(l.usage?.[today()]||0),limit=plan.dailySignals;return{key:l.key,status:l.status,plan:l.plan,planLabel:plan.label,dailyLimit:limit,usedToday:used,remainingToday:limit==null?null:Math.max(0,limit-used),expiresAt:l.expiresAt||null,customerName:l.customerName||'',email:l.email||'',deviceLimit:plan.deviceLimit,devices:(l.devices||[]).length,createdAt:l.createdAt}};function loadLicenses(){try{const rows=JSON.parse(fs.readFileSync(LICENSE_FILE,'utf8'));for(const l of Array.isArray(rows)?rows:[])if(l?.key)licenses.set(l.key,l)}catch{}}function saveLicenses(){try{fs.mkdirSync(DATA_DIR,{recursive:true});fs.writeFileSync(LICENSE_FILE,JSON.stringify([...licenses.values()],null,2))}catch{}}function sessionPayload(b){if(!isObj(b)||!only(b,['installationId','platform','scanning','version']))return null;if(!str(b.installationId,128)||('platform'in b&&b.platform!==null&&!str(b.platform,64))||('scanning'in b&&typeof b.scanning!=='boolean'||'version'in b&&b.version!==null&&!str(b.version,32)))return null;return{installationId:b.installationId,platform:b.platform??null,scanning:b.scanning??false,version:b.version??null}}function eventPayload(b){if(!isObj(b)||!only(b,['installationId','type','data']))return null;if(!str(b.installationId,128)||!str(b.type,64)||('data'in b&&!isObj(b.data)))return null;const data=b.data||{};if(JSON.stringify(data).length>8192)return null;return{installationId:b.installationId,type:b.type,data}}function licenseInput(b){if(!isObj(b)||!str(b.licenseKey,96)||!str(b.installationId,128))return null;return{licenseKey:b.licenseKey.trim().toUpperCase(),installationId:b.installationId,version:typeof b.version==='string'?b.version.slice(0,32):null,type:typeof b.type==='string'?b.type.slice(0,32):null}}function licenseCheck(p,{consume=false}={}){const l=licenses.get(p.licenseKey);if(!l)return{status:404,error:'license_not_found'};if(l.status!=='active')return{status:403,error:'license_inactive'};if(l.expiresAt&&Date.parse(l.expiresAt)<=now())return{status:403,error:'license_expired'};const plan=PLANS[l.plan]||PLANS.starter;l.devices??=[];let device=l.devices.find(x=>x.installationId===p.installationId);if(!device){if(l.devices.length>=plan.deviceLimit)return{status:403,error:'device_limit_reached'};device={installationId:p.installationId,createdAt:now(),lastSeen:now(),version:p.version||null};l.devices.push(device)}else{device.lastSeen=now();if(p.version)device.version=p.version}l.usage??={};const day=today(),used=Number(l.usage[day]||0),limit=plan.dailySignals;if(consume&&p.type==='signal'){if(limit!=null&&used>=limit)return{status:429,error:'daily_limit_reached',license:publicLicense(l)};l.usage[day]=used+1}l.updatedAt=now();saveLicenses();return{status:200,license:publicLicense(l)}}function newLicenseKey(){return`ATS-${crypto.randomBytes(3).toString('hex').toUpperCase()}-${crypto.randomBytes(3).toString('hex').toUpperCase()}-${crypto.randomBytes(2).toString('hex').toUpperCase()}`}function serveAdmin(res,file,type){try{const data=fs.readFileSync(path.join(ADMIN_DIR,file));res.writeHead(200,{'content-type':type,'cache-control':'no-store'});res.end(data);return true}catch{return false}}loadLicenses();const server=http.createServer(async(req,res)=>{if(req.headers.origin&&origin(req)===null)return json(req,res,403,{error:'origin_not_allowed'});if(req.method==='OPTIONS')return json(req,res,204,{});try{const url=new URL(req.url,'http://localhost'),pathname=url.pathname;if(pathname==='/admin'||pathname==='/admin/')return serveAdmin(res,'index.html','text/html; charset=utf-8')||json(req,res,404,{error:'admin_not_found'});if(pathname==='/admin/app.js')return serveAdmin(res,'app.js','text/javascript; charset=utf-8')||json(req,res,404,{error:'admin_asset_not_found'});if(pathname==='/admin/styles.css')return serveAdmin(res,'styles.css','text/css; charset=utf-8')||json(req,res,404,{error:'admin_asset_not_found'});if(pathname==='/health')return json(req,res,200,{ok:true,service:'ats-api',time:now(),authConfigured:!!API_KEY,adminAuthConfigured:!!ADMIN_KEY,licenses:licenses.size});const protectedRoute=(pathname==='/v1/session'&&req.method==='POST')||(pathname==='/v1/events'&&req.method==='POST');if(protectedRoute&&!auth(req))return json(req,res,API_KEY?401:503,{error:API_KEY?'unauthorized':'api_key_not_configured'});if(pathname.startsWith('/v1/admin/')&&!adminAuth(req))return json(req,res,ADMIN_KEY?401:503,{error:ADMIN_KEY?'unauthorized':'admin_key_not_configured'});if(pathname==='/v1/license/activate'&&req.method==='POST'){const p=licenseInput(await body(req));if(!p)return json(req,res,422,{error:'invalid_license_request'});const r=licenseCheck(p);return json(req,res,r.status,r.error?{ok:false,error:r.error,license:r.license}:{ok:true,license:r.license})}if(pathname==='/v1/license/validate'&&req.method==='POST'){const p=licenseInput(await body(req));if(!p)return json(req,res,422,{error:'invalid_license_request'});const r=licenseCheck(p);return json(req,res,r.status,r.error?{ok:false,error:r.error,license:r.license}:{ok:true,license:r.license})}if(pathname==='/v1/license/consume'&&req.method==='POST'){const p=licenseInput(await body(req));if(!p||p.type!=='signal')return json(req,res,422,{error:'invalid_usage_request'});const r=licenseCheck(p,{consume:true});return json(req,res,r.status,r.error?{ok:false,error:r.error,license:r.license}:{ok:true,license:r.license,usage:{dailyLimit:r.license.dailyLimit,usedToday:r.license.usedToday,remainingToday:r.license.remainingToday}})}if(pathname==='/v1/session'&&req.method==='POST'){const p=sessionPayload(await body(req));if(!p)return json(req,res,422,{error:'invalid_session'});sessions.set(p.installationId,{...sessions.get(p.installationId),...p,lastSeen:now()});return json(req,res,200,{ok:true,installationId:p.installationId})}if(pathname==='/v1/events'&&req.method==='POST'){const p=eventPayload(await body(req));if(!p)return json(req,res,422,{error:'invalid_event'});events.push({...p,id:crypto.randomUUID(),createdAt:now()});if(events.length>10000)events.shift();return json(req,res,201,{ok:true})}if(pathname==='/v1/admin/plans'&&req.method==='GET')return json(req,res,200,{plans:Object.values(PLANS)});if(pathname==='/v1/admin/licenses'&&req.method==='GET')return json(req,res,200,{licenses:[...licenses.values()].map(publicLicense).sort((a,b)=>b.createdAt-a.createdAt)});if(pathname==='/v1/admin/licenses'&&req.method==='POST'){const b=await body(req),plan=String(b.plan||'starter');if(!PLANS[plan])return json(req,res,422,{error:'invalid_plan'});const days=Number(b.days||30),createdAt=now(),l={key:newLicenseKey(),plan,status:'active',customerName:String(b.customerName||'').slice(0,120),email:String(b.email||'').slice(0,160),createdAt,updatedAt:createdAt,expiresAt:Number.isFinite(days)&&days>0?new Date(createdAt+days*86400000).toISOString():null,devices:[],usage:{}};licenses.set(l.key,l);saveLicenses();return json(req,res,201,{ok:true,license:publicLicense(l)})}const revoke=pathname.match(/^\/v1\/admin\/licenses\/([^/]+)\/(revoke|activate)$/);if(revoke&&req.method==='POST'){const key=decodeURIComponent(revoke[1]).toUpperCase(),l=licenses.get(key);if(!l)return json(req,res,404,{error:'license_not_found'});l.status=revoke[2]==='activate'?'active':'revoked';l.updatedAt=now();saveLicenses();return json(req,res,200,{ok:true,license:publicLicense(l)})}if(pathname==='/v1/admin/clients'&&req.method==='GET'){const clients=[];for(const l of licenses.values())for(const d of l.devices||[])clients.push({licenseKey:l.key,plan:l.plan,customerName:l.customerName||'',installationId:d.installationId,lastSeen:d.lastSeen||0,online:now()-(d.lastSeen||0)<60000,version:d.version||null});return json(req,res,200,{clients:clients.sort((a,b)=>b.lastSeen-a.lastSeen)})}if(pathname==='/v1/admin/metrics'&&req.method==='GET'){const ts=now(),active=[...sessions.values()].filter(s=>ts-s.lastSeen<60000),groups={};for(const s of active){const k=s.platform||'unknown';groups[k]=(groups[k]||0)+1}const lic=[...licenses.values()].map(publicLicense),clients=[];for(const l of licenses.values())for(const d of l.devices||[])clients.push(d);return json(req,res,200,{installations:sessions.size,online:active.length,licensedOnline:clients.filter(d=>ts-(d.lastSeen||0)<60000).length,scanners:active.filter(s=>s.scanning).length,events:events.length,licenses:lic.length,activeLicenses:lic.filter(x=>x.status==='active'&&(!x.expiresAt||Date.parse(x.expiresAt)>ts)).length,signalsToday:lic.reduce((n,x)=>n+x.usedToday,0),platforms:Object.entries(groups).map(([name,online])=>({name,online})),version:'0.4.0'})}return json(req,res,404,{error:'not_found'})}catch(e){return json(req,res,e.message==='too_large'?413:400,{error:e.message||'bad_request'})}});server.listen(PORT,()=>console.log(`ATS API :${PORT} • admin http://localhost:${PORT}/admin`));