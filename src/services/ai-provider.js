export class AIProvider{constructor({baseUrl='',enabled=false}={}){this.baseUrl=baseUrl;this.enabled=enabled}async review(candidate){if(!this.enabled||!this.baseUrl)return{available:false,action:'SKIP',reason:'AI disabled'};try{const r=await fetch(`${this.baseUrl.replace(/\/$/,'')}/v1/ai/review`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({candidate})});if(!r.ok)throw new Error('provider');return{available:true,...await r.json()}}catch{return{available:false,action:'SKIP',reason:'AI unavailable'}}}}
