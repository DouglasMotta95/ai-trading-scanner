const INSTALL_KEY='atsInstallationId';export async function installationId(){const x=await chrome.storage.local.get(INSTALL_KEY);if(x[INSTALL_KEY])return x[INSTALL_KEY];const id=crypto.randomUUID();await chrome.storage.local.set({[INSTALL_KEY]:id});return id}export async function heartbeat(state,settings={}){if(!settings.apiBase)return false;const id=await installationId();try{const r=await fetch(`${settings.apiBase.replace(/\/$/,'')}/v1/session`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({installationId:id,platform:state.platform||null,scanning:state.scanner==='scanning',version:chrome.runtime.getManifest().version})});return r.ok}catch{return false}}export async function track(type,data={},settings={}){if(!settings.apiBase)return false;const id=await installationId();try{return(await fetch(`${settings.apiBase.replace(/\/$/,'')}/v1/events`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({installationId:id,type,data})})).ok}catch{return false}}
