(() => {
  // Compatibility stub. The generic DOM adapter no longer guesses active assets or
  // publishes market snapshots. CasaTrade is handled by focused-asset-v2 + embedded feed.
  globalThis.__ATS_GENERIC_ADAPTER__ = true;
})();
