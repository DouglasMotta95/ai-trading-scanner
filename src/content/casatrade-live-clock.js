(() => {
  if(globalThis.__ATS_LIVE_CLOCK__)return;globalThis.__ATS_LIVE_CLOCK__=true;
  const send=message=>{const fn=globalThis.__ATS_SEND_MESSAGE__;if(typeof fn==='function')return fn(message);return new Promise(resolve=>{let tries=0;const retry=()=>{const current=globalThis.__ATS_SEND_MESSAGE__;if(typeof current==='function')current(message).then(resolve).catch(()=>resolve(null));else if(++tries<40)setTimeout(retry,50);else resolve(null);};retry();});};
  let anchor=null,timer=null,lastSent=-1;const nowFromAnchor=()=>anchor?anchor.epochAtAnchor+(performance.now()-anchor.perfAtAnchor):null;
  function schedule(){if(timer)return;timer=setTimeout(tick,120);}function tick(){timer=null;if(!anchor)return;const now=nowFromAnchor();if(!Number.isFinite(now)){anchor=null;lastSent=-1;return;}const ms=Math.max(0,anchor.closeAt-now),sec=Math.ceil(ms/1000);if(sec!==lastSent||ms<1500){lastSent=sec;send({type:'ATS_MARKET_CLOCK_V2',asset:anchor.asset,timeframe:anchor.timeframe,secondsRemaining:sec,millisecondsRemaining:Math.round(ms),closeAt:anchor.closeAt,expiration:anchor.expiration||null,available:true,verified:true,operational:true,clockRole:'candle-close',clockSource:'casatrade-platform-clock',clockMode:'ohlc-source-monotonic-anchor',clockText:'Fechamento ancorado exclusivamente no timestamp OHLC da CasaTrade',clockToken:`${sec}s`,confidence:98,sourceNow:Math.round(now)}).catch(()=>{});}if(ms<=0){anchor=null;lastSent=-1;return;}schedule();}
  window.addEventListener('ATS_NUMERIC_OHLC_CLOCK',event=>{const d=event.detail||{},openAt=Number(d.openAt),durationMs=Number(d.durationMs),sourceNow=Number(d.sourceNow);if(!d.asset||!d.timeframe||!Number.isFinite(openAt)||!Number.isFinite(durationMs)||durationMs<=0||!Number.isFinite(sourceNow))return;const closeAt=openAt+durationMs;if(sourceNow<openAt-1500||sourceNow>closeAt+1500)return;anchor={asset:d.asset,timeframe:d.timeframe,expiration:d.expiration||null,closeAt,epochAtAnchor:sourceNow,perfAtAnchor:performance.now()};lastSent=-1;if(timer){clearTimeout(timer);timer=null;}tick();},true);
})();
