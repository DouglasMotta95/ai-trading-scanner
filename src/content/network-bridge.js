(() => {
  // Compatibility stub. Embedded market data is relayed only by embedded-feed-bridge.js
  // into the single background-market-session authority.
  globalThis.__ATS_NETWORK_BRIDGE__ = true;
})();
