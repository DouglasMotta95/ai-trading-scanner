import{processSnapshot,resetOrchestrator}from'./core/orchestrator.js';import{detectPlatform}from'./platforms/registry.js';import{activateLicense,validateLicense,consumeSignal,clearLicense,licenseRequired}from'./services/license.js';const DEFAULT_LICENSE={status:'unconfigured',plan:null,planLabel:null,dailyLimit:null,usedToday:0,remainingToday:null,error:null},DEFAULT_STATE={connection:'offline',scanner:'idle',platformId:null,platformName:null,targetTabId:null,asset:null,marketType:'unknown',instrumentType:'unknown',timeframe:null,analysisTimeframe:null,expiration:null,targetExpiration:null,price:null,serverTime:null,capabilities:{structuredQuotes:false,candles:false,expiration:false,multiAsset:false},diagnostics:{},marketCatalog:{assets:[],timeframes:[],expirations:[],lines:[]},tradeIntent:null,license:DEFAULT_LICENSE,signal:null,lastSeen:null};let lastLicenseCheck=0;const merge=(state={},snapshot={})=>({...DEFAULT_STATE,...state,...snapshot,license:{...DEFAULT_LICENSE,...(state.license||{}),...(snapshot.license||{})},capabilities:{...DEFAULT_STATE.capabilities,...state.capabilities,...snapshot.capabilities},diagnostics:{...(state.diagnostics||{}),...(snapshot.diagnostics||{})}}),riskFrom=(s={},l={})=>({...s?.risk,signalsToday:l.usedToday??s?.risk?.signalsToday??0,maxSignals:l.dailyLimit??s?.risk?.maxSignals??s?.maxSignals??20,maxConsecutiveLosses:s?.risk?.maxConsecutiveLosses??s?.maxConsecutiveLosses,cooldownMs:s?.risk?.cooldownMs??s?.cooldownMs}),platformFromUrl=u=>{try{return detectPlatform(new URL(u).hostname)}catch{return null}},clean=v=>String(v??'').trim(),uniq=a=>[...new Set(a.filter(Boolean))],num=v=>v==null||v===''?null:Number.isFinite(Number(v))?Number(v):null,sameTarget=(s,sender)=>!s?.targetTabId||!sender?.tab?.id||s.targetTabId===sender.tab.id,catalogFrom=(candidates=[],prefs={})=>{const lines=[],seen=new Set();for(const c of candidates){const asset=clean(c?.asset);if(!asset)continue;const key=`${asset}|${clean(c.timeframe)}|${clean(c.expiration)}`;if(seen.has(key)){const i=lines.findIndex(x=>`${x.asset}|${clean(x.timeframe)}|${clean(x.expiration)}`===key),old=lines[i];if(old){if(old.price==null)old.price=num(c.price);if(old.bid==null)old.bid=num(c.bid);if(old.ask==null)old.ask=num(c.ask);if(old.payout==null)old.payout=num(c.payout);if(old.source==='dom'&&c.source)old.source=c.source}continue}seen.add(key);lines.push({asset,price:num(c.price),bid:num(c.bid),ask:num(c.ask),payout:num(c.payout),timeframe:clean(c.timeframe)||null,expiration:clean(c.expiration)||null,source:clean(c.source)||'observed'})}const timeframes=uniq(lines.map(x=>x.timeframe)),expirations=uniq(lines.map(x=>x.expiration));if(prefs.timeframe&&prefs.timeframe!=='AUTO'&&!timeframes.includes(prefs.timeframe))timeframes.unshift(prefs.timeframe);if(prefs.expiration&&prefs.expiration!=='AUTO'&&!expirations.includes(prefs.expiration))expirations.unshift(prefs.expiration);return{assets:uniq(lines.map(x=>x.asset)).sort(),timeframes,expirations,lines:lines.slice(0,100)}};const licenseError=r=>({status:r?.error==='license_expired'?'expired':r?.error==='daily_limit_reached'?'limit':'inactive',error:r?.error||'license_required'});async function syncLicense(settings={},state={},force=false){if(!force&&state.license?.status==='active'&&Date.now()-lastLicenseCheck<30000)return state.license;const r=await validateLicense(settings);lastLicenseCheck=Date.now();return r.ok?{...r.license,error:null}:{...DEFAULT_LICENSE,...licenseError(r),...(r.license||{})}}async function connectActiveTab(){const[tab]=await chrome.tabs.query({active:true,currentWindow:true});if(!tab?.id||!tab.url)return{ok:false,error:'active_tab_unavailable'};const platform=platformFromUrl(tab.url);if(!platform)return{ok:false,error:'platform_not_registered',host:(()=>{try{return new URL(tab.url).hostname}catch{return''}})()};await chrome.scripting.executeScript({target:{tabId:tab.id},files:['src/content/network-probe.js'],world:'MAIN'}).catch(()=>{});await chrome.scripting.executeScript({target:{tabId:tab.id},files:['src/content/network-bridge.js'],world:'ISOLATED'}).catch(()=>{});await chrome.scripting.executeScript({target:{tabId:tab.id},files:['src/content/generic-adapter.js'],world:'ISOLATED'}).catch(()=>{});const{scannerState}=await chrome.storage.local.get('scannerState'),next=merge(scannerState,{connection:'connecting',platformId:platform.id,platformName:platform.name,targetTabId:tab.id,marketCatalog:{assets:[],timeframes:[],expirations:[],lines:[]},diagnostics:{target:{host:new URL(tab.url).hostname,tabId:tab.id,connectedAt:Date.now()}}});await chrome.storage.local.set({scannerState:next});return{ok:true,platform:{id:platform.id,name:platform.name},tabId:tab.id}}async function prepareTrade(direction){if(!['BUY','SELL'].includes(direction))return{ok:false,error:'invalid_direction'};const{scannerState}=await chrome.storage.local.get('scannerState'),tabId=scannerState?.targetTabId;if(!tabId)return{ok:false,error:'platform_tab_not_connected'};const intent={direction,asset:scannerState.asset||null,timeframe:scannerState.analysisTimeframe||scannerState.timeframe||null,expiration:scannerState.targetExpiration||scannerState.expiration||null,createdAt:Date.now(),status:'prepared'};let handoff={found:false,label:null};try{const tab=await chrome.tabs.get(tabId);await chrome.tabs.update(tabId,{active:true});if(tab?.windowId!=null)await chrome.windows.update(tab.windowId,{focused:true}).catch(()=>{});await chrome.scripting.executeScript({target:{tabId},files:['src/content/trade-handoff.js'],world:'ISOLATED'}).catch(()=>{});handoff=await chrome.tabs.sendMessage(tabId,{type:'ATS_HIGHLIGHT_TRADE',...intent}).catch(()=>handoff)}catch{return{ok:false,error:'platform_tab_unavailable'}}const nextIntent={...intent,handoff:{found:!!handoff?.found,label:handoff?.label||null}};await chrome.storage.local.set({scannerState:merge(scannerState,{tradeIntent:nextIntent})});return{ok:true,intent:nextIntent}}chrome.runtime.onInstalled.addListener(async()=>{const{scannerState,settings={}}=await chrome.storage.local.get(['scannerState','settings']),updates={};if(!scannerState)updates.scannerState=DEFAULT_STATE;if(settings.casatradeSelectors&&!settings.selectorsByPlatform?.casatrade){const next={...settings,selectorsByPlatform:{...(settings.selectorsByPlatform||{}),casatrade:{...settings.casatradeSelectors}}};delete next.casatradeSelectors;updates.settings=next}if(Object.keys(updates).length)await chrome.storage.local.set(updates);await chrome.sidePanel.setPanelBehavior({openPanelOnActionClick:true})});chrome.runtime.onMessage.addListener((message,sender,sendResponse)=>{if(message?.type==='ATS_CONNECT_ACTIVE_TAB'){connectActiveTab().then(sendResponse).catch(e=>sendResponse({ok:false,error:String(e?.message||e)}));return true}if(message?.type==='ATS_ACTIVATE_LICENSE'){chrome.storage.local.get(['scannerState','settings']).then(async({scannerState,settings={}})=>{const r=await activateLicense(settings,message.key);lastLicenseCheck=0;const license=r.ok?{...r.license,error:null}:{...DEFAULT_LICENSE,...licenseError(r)},next=merge(scannerState,{license});await chrome.storage.local.set({scannerState:next});sendResponse({...r,license})}).catch(e=>sendResponse({ok:false,error:String(e?.message||e)}));return true}if(message?.type==='ATS_VALIDATE_LICENSE'){chrome.storage.local.get(['scannerState','settings']).then(async({scannerState,settings={}})=>{const license=await syncLicense(settings,scannerState,true),next=merge(scannerState,{license});await chrome.storage.local.set({scannerState:next});sendResponse({ok:license.status==='active',license})}).catch(e=>sendResponse({ok:false,error:String(e?.message||e)}));return true}if(message?.type==='ATS_CLEAR_LICENSE'){clearLicense().then(async()=>{const{scannerState}=await chrome.storage.local.get('scannerState');await chrome.storage.local.set({scannerState:merge(scannerState,{scanner:'idle',license:DEFAULT_LICENSE,signal:{state:'WAIT',score:0,grade:'—',confirmations:'0 / 6',direction:null,hint:'Licença removida.',provisional:true}})});sendResponse({ok:true})});return true}if(message?.type==='ATS_PREPARE_TRADE'){prepareTrade(String(message.direction||'').toUpperCase()).then(sendResponse).catch(e=>sendResponse({ok:false,error:String(e?.message||e)}));return true}if(message?.type==='ATS_CLEAR_TRADE_INTENT'){chrome.storage.local.get('scannerState').then(({scannerState})=>chrome.storage.local.set({scannerState:merge(scannerState,{tradeIntent:null})}).then(()=>sendResponse({ok:true})));return true}if(message?.type==='ATS_GET_PLATFORM_CONFIG'){let host=message.host||'';if(!host&&sender?.url)try{host=new URL(sender.url).hostname}catch{}const platform=detectPlatform(host);sendResponse({ok:!!platform,platform:platform?{...platform}:null});return}if(message?.type==='ATS_DOM_CATALOG'){const p=message.payload||{};chrome.storage.local.get(['scannerState','settings']).then(async({scannerState,settings={}})=>{if(!sameTarget(scannerState,sender))return sendResponse({ok:true,ignored:true});const prefs=settings.scanPreferences||{},domCatalog={candidates:Array.isArray(p.candidates)?p.candidates.slice(0,100):[],assetCount:Number(p.assetCount)||0,timeframe:p.timeframe||null,expiration:p.expiration||null,instrumentType:p.instrumentType||'unknown',lastSeen:Date.now()},net=scannerState?.diagnostics?.network?.candidates||[],marketCatalog=catalogFrom([...net,...domCatalog.candidates],prefs),next=merge(scannerState,{marketCatalog,diagnostics:{...(scannerState?.diagnostics||{}),domCatalog}});await chrome.storage.local.set({scannerState:next});sendResponse({ok:true,catalog:marketCatalog})}).catch(e=>sendResponse({ok:false,error:String(e?.message||e)}));return true}if(message?.type==='ATS_NETWORK_DIAGNOSTIC'){const p=message.payload||{};chrome.storage.local.get(['scannerState','settings']).then(async({scannerState,settings={}})=>{if(!sameTarget(scannerState,sender))return sendResponse({ok:true,ignored:true});let host='';try{host=new URL(sender?.url||'').hostname}catch{}const platform=detectPlatform(host),prefs=settings.scanPreferences||{},network={messages:p.messages||{},connections:p.connections||{},endpoints:Array.isArray(p.endpoints)?p.endpoints.slice(-20):[],keys:Array.isArray(p.keys)?p.keys.slice(0,120):[],candidates:Array.isArray(p.candidates)?p.candidates.slice(0,100).map(x=>({...x,source:'network'})):[],candidateCount:Number(p.candidateCount)||0,lastSeen:Date.now()},dom=scannerState?.diagnostics?.domCatalog?.candidates||[],marketCatalog=catalogFrom([...network.candidates,...dom],prefs),next=merge(scannerState,{platformId:scannerState?.platformId||platform?.id||null,platformName:scannerState?.platformName||platform?.name||null,marketCatalog,diagnostics:{...(scannerState?.diagnostics||{}),network}});await chrome.storage.local.set({scannerState:next});sendResponse({ok:true,catalog:marketCatalog})}).catch(e=>sendResponse({ok:false,error:String(e?.message||e)}));return true}if(message?.type==='ATS_PLATFORM_SNAPSHOT'){const snapshot=message.payload||{};chrome.storage.local.get(['scannerState','settings']).then(async({scannerState,settings={}})=>{if(!sameTarget(scannerState,sender))return sendResponse({ok:true,ignored:true});const prefs=settings.scanPreferences||{},analysisTimeframe=prefs.timeframe&&prefs.timeframe!=='AUTO'?prefs.timeframe:(snapshot.timeframe||null),targetExpiration=prefs.expiration&&prefs.expiration!=='AUTO'?prefs.expiration:(snapshot.expiration||null),enriched={...snapshot,analysisTimeframe,targetExpiration},license=await syncLicense(settings,scannerState);let activeScanner=scannerState?.scanner;if(licenseRequired(settings)&&license.status!=='active')activeScanner='idle';let next=merge(scannerState,{...enriched,scanner:activeScanner,license,connection:'online',lastSeen:Date.now()}),pipeline=processSnapshot(enriched,next,riskFrom(settings,license));next=merge(next,pipeline);const becameConfirm=next.signal?.state==='CONFIRM'&&scannerState?.signal?.state!=='CONFIRM';if(becameConfirm){const usage=await consumeSignal(settings);if(!usage.ok){next=merge(next,{license:{...license,...(usage.license||{}),...licenseError(usage)},signal:{...next.signal,state:'NO_TRADE',direction:null,hint:usage.error==='daily_limit_reached'?'Limite diário do plano atingido.':'Licença inválida para liberar novo sinal.'}})}else next=merge(next,{license:{...license,...(usage.license||{}),...(usage.usage||{}),status:'active',error:null}})}await chrome.storage.local.set({scannerState:next});sendResponse({ok:true,signal:next.signal,license:next.license})}).catch(e=>sendResponse({ok:false,error:String(e?.message||e)}));return true}if(message?.type==='ATS_GET_STATE'){chrome.storage.local.get('scannerState').then(({scannerState})=>sendResponse(merge(scannerState)));return true}if(message?.type==='ATS_SET_SCANNER'){chrome.storage.local.get(['scannerState','settings']).then(async({scannerState,settings={}})=>{const scanning=!!message.enabled,license=await syncLicense(settings,scannerState,true);if(scanning&&licenseRequired(settings)&&license.status!=='active'){const next=merge(scannerState,{scanner:'idle',license,signal:{state:'WAIT',score:0,grade:'—',confirmations:'0 / 6',direction:null,hint:'Ative uma licença válida para iniciar o scanner.',provisional:true}});await chrome.storage.local.set({scannerState:next});return sendResponse({ok:false,error:'license_required',state:next})}const next=merge(scannerState,{scanner:scanning?'scanning':'idle',license,signal:{state:scanning?'SEARCHING':'WAIT',score:0,grade:'—',confirmations:'0 / 6',direction:null,timeframe:scannerState?.analysisTimeframe||null,targetExpiration:scannerState?.targetExpiration||null,hint:scanning?'Iniciando leitura do mercado.':'Scanner pausado.',provisional:true}});await chrome.storage.local.set({scannerState:next});sendResponse({ok:true,state:next})});return true}if(message?.type==='ATS_RESET_STATE'){resetOrchestrator();chrome.storage.local.set({scannerState:DEFAULT_STATE}).then(()=>sendResponse({ok:true}));return true}});