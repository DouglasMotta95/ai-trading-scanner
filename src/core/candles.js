export class CandleBuilder{constructor(timeframeMs=60000){this.timeframeMs=timeframeMs;this.current=null;this.closed=[]}push(price,timestamp=Date.now()){price=Number(price);if(!Number.isFinite(price))return null;const bucket=Math.floor(timestamp/this.timeframeMs)*this.timeframeMs;if(!this.current||this.current.time!==bucket){const closed=this.current;this.current={time:bucket,open:price,high:price,low:price,close:price,ticks:1};if(closed){this.closed.push(Object.freeze({...closed}));if(this.closed.length>1000)this.closed.shift()}return closed||null}this.current.high=Math.max(this.current.high,price);this.current.low=Math.min(this.current.low,price);this.current.close=price;this.current.ticks++;return null}snapshot(){return{current:this.current?{...this.current}:null,closed:[...this.closed]}}}
export const TIMEFRAMES={M1:60000,M5:300000,M15:900000,M30:1800000,H1:3600000,H4:14400000};
