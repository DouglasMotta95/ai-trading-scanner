import{CandleBuilder,TIMEFRAMES}from'./candles.js';import{analyzeCandles}from'./analysis.js';import{confluence}from'./confluence.js';import{marketRegime,qualityGate}from'./market-regime.js';import{tradingGuard,dedupeSignal}from'./risk-controls.js';import{nextSignalState}from'./signal-machine.js';const builders=new Map(),lastSignals=new Map(),frame=s=>String(s.analysisTimeframe||s.timeframe||'M1').toUpperCase(),key=s=>`${s.platformId||'unknown'}:${s.asset||'unknown'}:${frame(s)}`,tf=s=>TIMEFRAMES[frame(s)]||TIMEFRAMES.M1;const priceOf=v=>{if(Number.isFinite(v))return Number(v);let s=String(v??'').trim().replace(/\s/g,'').replace(/[^\d,.-]/g,'');if(s.includes(',')&&s.includes('.'))s=s.lastIndexOf(',')>s.lastIndexOf('.')?s.replace(/\./g,'').replace(',','.'):s.replace(/,/g,'');else s=s.replace(',','.');const n=Number(s);return Number.isFinite(n)?n:null};const aligned=(direction,a,b)=>direction==='BUY'?a>b:direction==='SELL'?a<b:false,warm=(state,connected,candles,timeframe,targetExpiration,structured)=>({signal:{state:state.scanner==='scanning'?'SEARCHING':'WAIT',score:0,grade:'—',confirmations:'0 / 6',direction:null,timeframe,targetExpiration,hint:!connected?'Aguardando preço válido.':state.scanner!=='scanning'?'Scanner pronto.':`Aquecendo motor • ${Math.min(candles.length,21)}/21 candles ${structured?'estruturados':'provisórios'}.`,provisional:!structured,candleCount:candles.length,warmup:{current:Math.min(candles.length,21),required:21}}});export function resetOrchestrator(){builders.clear();lastSignals.clear()}export function processSnapshot(snapshot={},state={},risk={}){const price=priceOf(snapshot.price),connected=price!==null,k=key(snapshot),analysisTimeframe=frame(snapshot),targetExpiration=snapshot.targetExpiration||snapshot.expiration||null,structured=!!snapshot.capabilities?.structuredQuotes;if(!builders.has(k))builders.set(k,new CandleBuilder(tf(snapshot)));const builder=builders.get(k),closed=connected?builder.push(price,Number(snapshot.serverTime)||Date.now()):null,candles=builder.snapshot().closed;if(candles.length<21)return warm(state,connected,candles,analysisTimeframe,targetExpiration,structured);if(!closed&&state.signal?.updatedAt)return{signal:{...state.signal,timeframe:analysisTimeframe,targetExpiration,candleCount:candles.length,provisional:!structured,warmup:{current:21,required:21}}};const analysis=analyzeCandles(candles),regime=marketRegime(candles),rsi=analysis.indicators?.rsi14,macd=analysis.indicators?.macd?.histogram,e9=analysis.indicators?.ema9,e21=analysis.indicators?.ema21,d=analysis.direction,checks=[{label:'Direção quantitativa',weight:20,passed:!!d},{label:'Tendência EMA alinhada',weight:20,passed:aligned(d,e9,e21)},{label:'RSI compatível',weight:20,passed:d==='BUY'?rsi>=50&&rsi<75:d==='SELL'?rsi<=50&&rsi>25:false},{label:'MACD alinhado',weight:20,passed:d==='BUY'?macd>0:d==='SELL'?macd<0:false},{label:'Regime identificado',weight:10,passed:regime.type!=='unknown'},{label:'Força quantitativa',weight:10,passed:analysis.score>=70}],cf=confluence(checks),gate=qualityGate({connected,stale:false,regime,dataQuality:structured?1:.85}),guard=tradingGuard(risk),candidate={asset:snapshot.asset,direction:d,createdAt:Date.now()},duplicate=!!d&&dedupeSignal(lastSignals.get(k),candidate,risk.cooldownMs||60000),baseAllowed=gate.allowed&&guard.allowed&&!duplicate,provisionalWatch=!structured&&baseAllowed&&!!d&&cf.score>=70,allowed=structured&&baseAllowed,finalAnalysis={...analysis,score:cf.score,state:allowed?(cf.score>=70?'WATCH':'WAIT'):provisionalWatch?'WATCH':'NO_TRADE'},machine=provisionalWatch?{state:'WATCH',label:`${d} • PRÉ-SINAL PROVISÓRIO`}:nextSignalState(state.signal?.state,finalAnalysis,{connected,scanning:state.scanner==='scanning',stale:false}),blocked=[...gate.reasons,...guard.reasons,duplicate?'Sinal duplicado em cooldown':null,!structured&&!provisionalWatch?'Feed estruturado ainda não validado':null].filter(Boolean);if(allowed&&machine.state==='CONFIRM')lastSignals.set(k,candidate);return{signal:{state:machine.state,score:baseAllowed?cf.score:0,rawScore:cf.score,grade:baseAllowed?cf.grade:'—',confirmations:`${cf.confirmations} / ${cf.total}`,direction:baseAllowed?d:null,timeframe:analysisTimeframe,targetExpiration,hint:provisionalWatch?`${machine.label} • aguardando validação do feed antes de confirmar entrada.`:allowed?machine.label:blocked.join(' • '),reasons:analysis.reasons,regime:regime.type,provisional:!structured,candleCount:candles.length,warmup:{current:21,required:21},updatedAt:Date.now()}}}