import{CandleBuilder,TIMEFRAMES}from'./candles.js';import{analyzeCandles}from'./analysis.js';import{confluence}from'./confluence.js';import{marketRegime,qualityGate}from'./market-regime.js';import{tradingGuard,dedupeSignal}from'./risk-controls.js';import{nextSignalState}from'./signal-machine.js';const builders=new Map(),lastSignals=new Map();const key=s=>`${s.platformId||'unknown'}:${s.asset||'unknown'}:${String(s.timeframe||'M1').toUpperCase()}`,tf=s=>TIMEFRAMES[String(s.timeframe||'M1').toUpperCase()]||TIMEFRAMES.M1;const priceOf=v=>{if(Number.isFinite(v))return Number(v);let s=String(v??'').trim().replace(/\s/g,'').replace(/[^\d,.-]/g,'');if(s.includes(',')&&s.includes('.'))s=s.lastIndexOf(',')>s.lastIndexOf('.')?s.replace(/\./g,'').replace(',','.'):s.replace(/,/g,'');else s=s.replace(',','.');const n=Number(s);return Number.isFinite(n)?n:null};const aligned=(direction,a,b)=>direction==='BUY'?a>b:direction==='SELL'?a<b:false;export function resetOrchestrator(){builders.clear();lastSignals.clear()}export function processSnapshot(snapshot={},state={},risk={}){const price=priceOf(snapshot.price),connected=price!==null,k=key(snapshot);if(!builders.has(k))builders.set(k,new CandleBuilder(tf(snapshot)));const builder=builders.get(k),closed=connected?builder.push(price,Number(snapshot.serverTime)||Date.now()):null,candles=builder.snapshot().closed;if(!closed){if(state.signal?.updatedAt)return{signal:{...state.signal,candleCount:candles.length,provisional:!snapshot.capabilities?.structuredQuotes}};return{signal:{state:state.scanner==='scanning'?'SEARCHING':'WAIT',score:0,grade:'—',confirmations:'0 / 0',direction:null,hint:connected?'Construindo candles locais para análise.':'Aguardando preço válido.',provisional:true,candleCount:candles.length}}}const analysis=analyzeCandles(candles),regime=marketRegime(candles),rsi=analysis.indicators?.rsi14,macd=analysis.indicators?.macd?.histogram,e9=analysis.indicators?.ema9,e21=analysis.indicators?.ema21,d=analysis.direction,checks=[{label:'Direção quantitativa',weight:20,passed:!!d},{label:'Tendência EMA alinhada',weight:20,passed:aligned(d,e9,e21)},{label:'RSI compatível',weight:20,passed:d==='BUY'?rsi>=50&&rsi<75:d==='SELL'?rsi<=50&&rsi>25:false},{label:'MACD alinhado',weight:20,passed:d==='BUY'?macd>0:d==='SELL'?macd<0:false},{label:'Regime identificado',weight:10,passed:regime.type!=='unknown'},{label:'Força quantitativa',weight:10,passed:analysis.score>=70}],cf=confluence(checks),structured=!!snapshot.capabilities?.structuredQuotes,dataQuality=structured?1:.6,gate=qualityGate({connected,stale:false,regime,dataQuality}),guard=tradingGuard(risk),candidate={asset:snapshot.asset,direction:d,createdAt:Date.now()},duplicate=!!d&&dedupeSignal(lastSignals.get(k),candidate,risk.cooldownMs||60000),allowed=gate.allowed&&guard.allowed&&!duplicate,finalAnalysis={...analysis,score:cf.score,state:allowed?(cf.score>=70?'WATCH':'WAIT'):'NO_TRADE'},machine=nextSignalState(state.signal?.state,finalAnalysis,{connected,scanning:state.scanner==='scanning',stale:false}),blocked=[...gate.reasons,...guard.reasons,duplicate?'Sinal duplicado em cooldown':null,!structured?'Fonte estruturada ainda não validada':null].filter(Boolean);if(allowed&&machine.state==='CONFIRM')lastSignals.set(k,candidate);return{signal:{state:machine.state,score:allowed?cf.score:0,rawScore:cf.score,grade:allowed?cf.grade:'—',confirmations:`${cf.confirmations} / ${cf.total}`,direction:allowed?d:null,hint:allowed?machine.label:blocked.join(' • '),reasons:analysis.reasons,regime:regime.type,provisional:!structured,candleCount:candles.length,updatedAt:Date.now()}}}