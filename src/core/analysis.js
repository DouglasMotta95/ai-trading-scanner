import{ema,rsi,macd,atr,bollinger}from'./indicators.js';export{ema,rsi};export function analyzeCandles(candles=[]){if(candles.length<21)return{state:'NO_TRADE',score:0,direction:null,reasons:['Dados insuficientes'],indicators:{}};const closes=candles.map(c=>Number(c.close)).filter(Number.isFinite);if(closes.length<21)return{state:'NO_TRADE',score:0,direction:null,reasons:['Candles inválidos'],indicators:{}};const e9=ema(closes,9),e21=ema(closes,21),momentum=rsi(closes,14),m=macd(closes),a=atr(candles),bb=bollinger(closes),last=closes.at(-1);let buy=0,sell=0;const reasons=[];if(e9!=null&&e21!=null){if(e9>e21){buy+=30;reasons.push('EMA 9 acima da EMA 21')}else{sell+=30;reasons.push('EMA 9 abaixo da EMA 21')}}if(e9!=null){last>e9?buy+=20:sell+=20}if(momentum>=52&&momentum<=72){buy+=25;reasons.push('Momentum comprador saudável')}if(momentum<=48&&momentum>=28){sell+=25;reasons.push('Momentum vendedor saudável')}if(m?.histogram>0)buy+=10;if(m?.histogram<0)sell+=10;const direction=buy===sell?null:buy>sell?'BUY':'SELL',score=Math.max(buy,sell);return{state:score>=70?'WATCH':'WAIT',score,direction,reasons,indicators:{ema9:e9,ema21:e21,rsi14:momentum,macd:m,atr14:a,bollinger:bb}}}