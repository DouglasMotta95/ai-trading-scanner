export function tradingGuard({signalsToday=0,consecutiveLosses=0,maxSignals=20,maxConsecutiveLosses=3,cooldownUntil=0}={}){const reasons=[];if(signalsToday>=maxSignals)reasons.push('Limite diário de sinais');if(consecutiveLosses>=maxConsecutiveLosses)reasons.push('Cooldown por sequência de perdas');if(Date.now()<cooldownUntil)reasons.push('Cooldown ativo');return{allowed:reasons.length===0,reasons}}
export function dedupeSignal(last,candidate,cooldownMs=60000){if(!last)return false;return last.asset===candidate.asset&&last.direction===candidate.direction&&Date.now()-last.createdAt<cooldownMs}
